# Copy to .env (gitignored). Loaded by intellifi.config at import time.
POLYGONSCAN_API_KEY=            # free: polygonscan.com/myapikey  (on-chain transfers, scripts/03)
DUNE_API_KEY=                   # free: dune.com/settings/api     (fill reconstruction, scripts/08)
